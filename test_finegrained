# 7. Récupérer explicitement les fonctions FP8 locales

import importlib

matmul_module = importlib.import_module(
    "local_finegrained_fp8.matmul"
)

batched_module = importlib.import_module(
    "local_finegrained_fp8.batched"
)

grouped_module = importlib.import_module(
    "local_finegrained_fp8.grouped"
)

print("=== MATMUL ===")
print("module :", matmul_module)
print("fonctions :", [x for x in dir(matmul_module) if "matmul" in x.lower()])

print("\n=== BATCHED ===")
print("module :", batched_module)
print("fonctions :", [x for x in dir(batched_module) if "matmul" in x.lower()])

print("\n=== GROUPED ===")
print("module :", grouped_module)
print("fonctions :", [x for x in dir(grouped_module) if "matmul" in x.lower()])
